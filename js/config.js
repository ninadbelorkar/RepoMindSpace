// config.js — Central API base URL
// Flask serves both the frontend and backend from the same server,
// so all API calls use relative URLs (empty string = same origin).
// Works identically in local dev (http://localhost:5000) and on Render.

const API_BASE = '';
